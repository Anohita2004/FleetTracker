<mvc:View
  controllerName="com.locationtracker.locationtracker.controller.App"
  xmlns:mvc="sap.ui.core.mvc"
  xmlns="sap.m"
  xmlns:core="sap.ui.core"
  xmlns:tnt="sap.tnt"
  xmlns:form="sap.ui.layout.form"
  xmlns:customData="http://schemas.sap.com/sapui5/extension/sap.ui.core.CustomData/1"
  displayBlock="true">
  <App id="app">
    <pages>
      <Page showHeader="false">
        <content>
          <VBox class="trackerPage">
            <VBox visible="{= ${appState>/sCurrentView} === 'loading' }" class="stateShell">
              <VBox class="centerCard" alignItems="Center" justifyContent="Center">
                <BusyIndicator size="2rem" />
                <Text text="Checking authentication status..." class="sapUiSmallMarginTop" />
              </VBox>
            </VBox>

            <VBox visible="{= ${appState>/sCurrentView} === 'error401' }" class="stateShell">
              <VBox class="centerCard" alignItems="Center" justifyContent="Center">
                <Title text="Access Denied" level="H3" />
                <Text text="Your session is not authorized to access this application." class="sapUiSmallMarginTop" />
                <Button text="Go to Login" type="Emphasized" press=".onGoToLogin" class="sapUiSmallMarginTop" />
              </VBox>
            </VBox>

            <!-- ── Login — split-screen ──────────────────────────────── -->
            <VBox visible="{= ${appState>/sCurrentView} === 'loginPage' }" class="ftLoginShell">
              <HBox class="ftLoginCard" wrap="Wrap">

                <!-- Left: brand panel -->
                <VBox class="ftLoginBrand">
                  <HBox class="ftLoginLogo" alignItems="Center">
                    <core:Icon src="sap-icon://shipping-status" class="ftLoginLogoIcon" />
                    <Title text="FleetTracker" level="H2" class="ftLoginLogoTitle" />
                  </HBox>
                  <Text text="Track every vehicle, trip, and checkpoint in real time." class="ftLoginTagline" />
                  <VBox class="ftLoginFeatures">
                    <HBox class="ftLoginFeatureRow" alignItems="Center">
                      <core:Icon src="sap-icon://map" class="ftLoginFeatureIcon" />
                      <Text text="Live GPS across your fleet" class="ftLoginFeatureText" />
                    </HBox>
                    <HBox class="ftLoginFeatureRow" alignItems="Center">
                      <core:Icon src="sap-icon://document-list" class="ftLoginFeatureIcon" />
                      <Text text="Freight orders and checkpoints" class="ftLoginFeatureText" />
                    </HBox>
                    <HBox class="ftLoginFeatureRow" alignItems="Center">
                      <core:Icon src="sap-icon://key" class="ftLoginFeatureIcon" />
                      <Text text="Gate-pass and access control" class="ftLoginFeatureText" />
                    </HBox>
                  </VBox>
                </VBox>

                <!-- Right: form panel -->
                <VBox class="ftLoginForm">
                  <Title text="Welcome back" level="H3" class="ftLoginFormTitle" />
                  <Text text="Choose how you want to sign in." class="ftLoginFormSubtitle" />
                  <IconTabBar
                    expandable="false"
                    selectedKey="{appState>/loginTab}"
                    select=".onLoginTabSelect"
                    class="ftRoleTabs">
                    <items>
                      <IconTabFilter text="Admin" key="admin" icon="sap-icon://manager">
                        <VBox class="ftLoginTabContent">
                          <Text text="Use your FleetAdmin credentials." class="ftLoginSsoHint" />
                          <Button text="Sign in as admin" icon="sap-icon://log" type="Emphasized"
                                  press=".onAdminLoginPress" class="ftLoginBtn" />
                        </VBox>
                      </IconTabFilter>
                      <IconTabFilter text="Driver" key="driver" icon="sap-icon://employee">
                        <VBox class="ftLoginTabContent">
                          <VBox class="ftLoginField">
                            <Label text="Email" />
                            <Input value="{appState>/driverLogin/email}" type="Email"
                                   placeholder="you@company.com" width="100%" class="ftLoginInput" />
                          </VBox>
                          <VBox class="ftLoginField">
                            <Label text="Password" />
                            <Input value="{appState>/driverLogin/password}" type="Password"
                                   placeholder="Enter your password" width="100%" class="ftLoginInput" />
                          </VBox>
                          <Button text="Sign in" icon="sap-icon://log" type="Emphasized"
                                  press=".onDriverLoginPress" class="ftLoginBtn" />
                        </VBox>
                      </IconTabFilter>
                    </items>
                  </IconTabBar>
                  <MessageStrip
                    text="{appState>/authError}"
                    type="Error"
                    showIcon="true"
                    showCloseButton="false"
                    visible="{= !!${appState>/authError} }"
                    class="ftLoginError" />
                  <HBox class="ftLoginStatus" alignItems="Center">
                    <VBox class="ftStatusDot" />
                    <Text text="System status: not authenticated" class="ftStatusText" />
                  </HBox>
                </VBox>

              </HBox>
            </VBox>

            <!-- ═══════════════════════════════════════════════════════════
                 ADMIN DASHBOARD — dark sidebar layout
                 ═══════════════════════════════════════════════════════════ -->
            <HBox visible="{= ${appState>/sCurrentView} === 'adminDashboard' }" class="adminShell">

              <!-- ── Left sidebar ─────────────────────────────────────── -->
              <VBox class="adminSidebar">
                <HBox class="adminBrand" alignItems="Center">
                  <core:Icon src="sap-icon://fleet" class="adminBrandIcon" />
                  <Title text="FleetTracker" level="H5" class="adminBrandTitle" />
                </HBox>
                <tnt:NavigationList class="adminNavList">
                  <tnt:NavigationListItem text="Dashboard" icon="sap-icon://home" selected="true" />
                  <tnt:NavigationListItem text="Drivers" icon="sap-icon://employee" />
                  <tnt:NavigationListItem text="Vehicles" icon="sap-icon://fleet" press=".onNavigateToFleetOverview" />
                  <tnt:NavigationListItem text="Freight orders" icon="sap-icon://document-list" press=".onNavigateToFreightOrders" />
                  <tnt:NavigationListItem text="Gate pass" icon="sap-icon://key" press=".onNavigateToGatePasses" />
                  <tnt:NavigationListItem text="Alerts" icon="sap-icon://alert" />
                  <tnt:NavigationListItem text="Reports" icon="sap-icon://bar-chart" />
                </tnt:NavigationList>
                <VBox class="adminSideGrow" />
                <tnt:NavigationList class="adminNavListFixed">
                  <tnt:NavigationListItem icon="sap-icon://employee" text="{appState>/adminProfile/name}" />
                  <tnt:NavigationListItem icon="sap-icon://log" text="Logout" press=".onLogoutPress" />
                </tnt:NavigationList>
              </VBox>

              <!-- ── Right main area ──────────────────────────────────── -->
              <VBox class="adminMain">

                <!-- Top bar -->
                <Toolbar class="adminTopBar">
                  <Title text="Dashboard" level="H4" class="adminTopTitle" />
                  <ToolbarSpacer />
                  <Button icon="sap-icon://bell" type="Transparent" tooltip="Notifications" />
                  <Button text="Manage fleet" icon="sap-icon://shipping-status" type="Emphasized" press=".onNavigateToFleetOverview" />
                </Toolbar>

                <VBox class="adminContent">

                  <!-- KPI row — 4 cards -->
                  <HBox wrap="Wrap" class="dashRow">
                    <VBox class="dashKpiCard">
                      <VBox class="kpiChip kpiChipBlue" justifyContent="Center" alignItems="Center">
                        <core:Icon src="sap-icon://group" class="kpiChipIcon kpiChipIconBlue" />
                      </VBox>
                      <Text text="{appState>/driverSummary/total}" class="kpiNum kpiNumBlue" />
                      <Text text="Total drivers" class="kpiLbl" />
                    </VBox>
                    <VBox class="dashKpiCard">
                      <VBox class="kpiChip kpiChipGreen" justifyContent="Center" alignItems="Center">
                        <core:Icon src="sap-icon://employee-approvals" class="kpiChipIcon kpiChipIconGreen" />
                      </VBox>
                      <Text text="{appState>/driverSummary/active}" class="kpiNum kpiNumGreen" />
                      <Text text="Active drivers" class="kpiLbl" />
                    </VBox>
                    <VBox class="dashKpiCard">
                      <VBox class="kpiChip kpiChipAmber" justifyContent="Center" alignItems="Center">
                        <core:Icon src="sap-icon://shipping-status" class="kpiChipIcon kpiChipIconAmber" />
                      </VBox>
                      <Text text="{appState>/driverSummary/onTrip}" class="kpiNum kpiNumAmber" />
                      <Text text="On trip" class="kpiLbl" />
                    </VBox>
                    <VBox class="dashKpiCard">
                      <VBox class="kpiChip kpiChipRed" justifyContent="Center" alignItems="Center">
                        <core:Icon src="sap-icon://alert" class="kpiChipIcon kpiChipIconRed" />
                      </VBox>
                      <Text text="{appState>/pendingDriverCount}" class="kpiNum kpiNumRed" />
                      <Text text="Pending approval" class="kpiLbl" />
                    </VBox>
                  </HBox>

                  <!-- Drivers card -->
                  <VBox class="dashCard">
                    <HBox justifyContent="SpaceBetween" alignItems="Center" class="dashCardHdr">
                      <Title text="Drivers" level="H4" />
                      <SearchField placeholder="Search name, email or licence…" width="18rem" liveChange=".onDriverSearch" />
                    </HBox>
                    <Table items="{appState>/drivers}" inset="false" mode="SingleSelectMaster"
                           selectionChange=".onSelectDriver" noDataText="No drivers found">
                      <columns>
                        <Column><Text text="Name" /></Column>
                        <Column><Text text="Email" /></Column>
                        <Column><Text text="Vehicle ID" /></Column>
                        <Column hAlign="Center"><Text text="Status" /></Column>
                        <Column hAlign="Center"><Text text="Activity" /></Column>
                        <Column><Text text="License #" /></Column>
                        <Column hAlign="Center"><Text text="Reg. status" /></Column>
                        <Column hAlign="Center"><Text text="Action" /></Column>
                      </columns>
                      <items>
                        <ColumnListItem>
                          <cells>
                            <HBox alignItems="Center">
                              <VBox class="driverAvatar" justifyContent="Center" alignItems="Center">
                                <Text text="{path: 'appState>name', formatter: '.formatInitials'}" class="driverAvatarText" />
                              </VBox>
                              <Text text="{appState>name}" />
                            </HBox>
                            <Text text="{appState>email}" />
                            <Text text="{appState>vehicleId}" />
                            <ObjectStatus
                              text="{= ${appState>isActive} ? 'Active' : 'Inactive' }"
                              state="{= ${appState>isActive} ? 'Success' : 'Warning' }" />
                            <ObjectStatus
                              text="{appState>activityStatus}"
                              state="{= ${appState>activityStatus} === 'On Trip' ? 'Success' : 'None' }" />
                            <Text text="{appState>licenseNumber}" />
                            <ObjectStatus
                              text="{appState>registrationStatus}"
                              state="{= ${appState>registrationStatus} === 'APPROVED' ? 'Success' : ${appState>registrationStatus} === 'PENDING' ? 'Warning' : 'Error' }" />
                            <HBox>
                              <Button icon="sap-icon://decline" type="Transparent" press=".onDeleteDriver"
                                      visible="{= ${appState>isActive} }" tooltip="Deactivate" />
                              <Button icon="sap-icon://activate" type="Transparent" press=".onReactivateDriver"
                                      visible="{= !${appState>isActive} }" tooltip="Reactivate" />
                              <Button icon="sap-icon://delete" type="Transparent" press=".onPermanentlyDeleteDriver"
                                      visible="{= !${appState>isActive} }" tooltip="Permanently Delete" />
                            </HBox>
                          </cells>
                        </ColumnListItem>
                      </items>
                    </Table>
                  </VBox>


                  <!-- Current trip / Live position / Captured points — admin only -->
                  <HBox wrap="Wrap" class="cardRow kpiRow">
                    <Panel headerText="Current trip" class="metricCard kpiCard">
                      <content>
                        <VBox class="metricContent">
                          <HBox justifyContent="SpaceBetween" alignItems="Center">
                            <ObjectStatus title="Title" text="{= ${appState>/currentTrip/title} || 'No active trip'}" />
                            <core:Icon src="sap-icon://journey-change" class="kpiIcon" />
                          </HBox>
                          <ObjectStatus title="Status" text="{= ${appState>/currentTrip/status} || 'IDLE'}" state="{= ${appState>/currentTrip/status} === 'ACTIVE' ? 'Success' : 'Information' }" />
                          <ObjectStatus title="Started At" text="{= ${appState>/currentTrip/startedAt} || '-'}" />
                        </VBox>
                      </content>
                    </Panel>

                    <Panel headerText="Live position" class="metricCard kpiCard">
                      <content>
                        <VBox class="metricContent">
                          <HBox justifyContent="SpaceBetween" alignItems="Center">
                            <Title text="Latest Coordinates" level="H5" />
                            <core:Icon src="sap-icon://map-3" class="kpiIcon" />
                          </HBox>
                          <ObjectStatus title="Latitude" text="{= ${appState>/lastPoint/latitude} || '-'}" />
                          <ObjectStatus title="Longitude" text="{= ${appState>/lastPoint/longitude} || '-'}" />
                          <ObjectStatus title="Accuracy (m)" text="{= ${appState>/lastPoint/accuracy} || '-'}" />
                        </VBox>
                      </content>
                    </Panel>

                    <Panel headerText="Captured points" class="metricCard kpiCard">
                      <content>
                        <VBox class="metricContent">
                          <HBox justifyContent="SpaceBetween" alignItems="Center">
                            <Title text="Path Density" level="H5" />
                            <core:Icon src="sap-icon://line-chart" class="kpiIcon" />
                          </HBox>
                          <ObjectNumber number="{appState>/totalPoints}" unit="pts" />
                          <ProgressIndicator percentValue="{= ${appState>/totalPoints} > 500 ? 100 : ${appState>/totalPoints} / 5 }" displayValue="{= ${appState>/totalPoints} + ' points captured'}" state="Success" />
                        </VBox>
                      </content>
                    </Panel>
                  </HBox>

                  <!-- Route map — inline so it sits inside the sidebar layout -->
                  <Panel headerText="Route Map" class="mapPanel">
                    <content>
                      <VBox class="mapShell">
                        <core:HTML id="trackerMap" content="&lt;div class='trackerMap' style='width:100%;height:340px;'&gt;&lt;/div&gt;" class="trackerMap" />
                      </VBox>
                    </content>
                  </Panel>

                  <!-- Performance metrics -->
                  <Panel headerText="Performance Metrics" class="metricCard metricsPanel">
                    <content>
                      <HBox wrap="Wrap" class="cardRow">
                        <VBox class="metricContent metricColumn">
                          <ObjectStatus title="Total Trips" text="{appState>/metrics/totalTrips}" />
                          <ObjectStatus title="Completed Trips" text="{appState>/metrics/completedTrips}" />
                          <ObjectStatus title="Completion Rate" text="{= ${appState>/metrics/completionRate} + '%'}" state="Success" />
                          <ObjectStatus title="Avg Points / Trip" text="{appState>/metrics/avgPointsPerTrip}" />
                          <ObjectStatus title="Avg GPS Accuracy (m)" text="{appState>/metrics/avgGpsAccuracy}" />
                        </VBox>
                        <VBox class="metricContent metricColumn">
                          <ObjectStatus title="Avg Session Duration (ms)" text="{appState>/metrics/avgSessionDurationMs}" />
                          <ObjectStatus title="Backend Ingest Success" text="{= ${appState>/metrics/ingestSuccessRate} + '%'}" state="Success" />
                          <ObjectStatus title="Avg Backend Ingest Latency (ms)" text="{appState>/metrics/avgIngestLatencyMs}" />
                          <ObjectStatus title="Avg Client Update Latency (ms)" text="{appState>/metrics/avgClientUpdateLatencyMs}" />
                          <ObjectStatus title="Latest Client Update Latency (ms)" text="{appState>/metrics/latestClientUpdateLatencyMs}" />
                        </VBox>
                      </HBox>
                    </content>
                  </Panel>

                </VBox><!-- /adminContent -->
              </VBox><!-- /adminMain -->
            </HBox><!-- /adminShell -->

            <VBox visible="{= ${appState>/sCurrentView} === 'driverDashboard' }">
              <Toolbar class="headerBar">
                <Title text="FleetTracker" />
                <ToolbarSpacer />
                <Text text="{appState>/driverProfile/name}" />
                <Button text="Logout" type="Transparent" press=".onLogoutPress" />
              </Toolbar>

              <VBox class="heroSection">
                <HBox class="heroRow" wrap="Wrap" justifyContent="SpaceBetween" alignItems="Center">
                  <VBox class="heroText">
                    <Title text="Driver Control Center" level="H2" />
                    <Text text="Start, stop, and monitor your trip in real time." class="heroSubtitle" />
                  </VBox>
                  <HBox wrap="Wrap" class="buttonCluster">
                    <Button text="{= ${appState>/currentTrip/ID} ? 'Resume Tracking' : 'Start Trip' }" type="Emphasized" press=".onStartTracking" visible="{= !${appState>/tracking} }" />
                    <Button text="Stop Trip" type="Reject" press=".onStopTracking" visible="{appState>/tracking}" />
                    <Button text="Refresh Path" icon="sap-icon://refresh" press=".onRefreshPath" />
                  </HBox>
                </HBox>
                <HBox class="statusStrip" wrap="Wrap" alignItems="Center">
                  <ObjectStatus title="System Status" text="{appState>/statusText}" state="{= ${appState>/tracking} ? 'Success' : 'Information' }" />
                  <ObjectStatus title="Location Permission" text="{appState>/permissionText}" state="{= ${appState>/tracking} ? 'Success' : 'Warning' }" />
                </HBox>
              </VBox>
            </VBox>

            <!-- Driver-only section: trip info cards + performance metrics (admin gets these inline above) -->
            <VBox visible="{= ${appState>/sCurrentView} === 'driverDashboard' }" class="sharedMonitorSection">
              <!-- Trip cards shown here only for driver; admin gets them inline above -->
              <HBox wrap="Wrap" class="cardRow kpiRow" visible="{= ${appState>/sCurrentView} === 'driverDashboard' }">
                <Panel headerText="Current Trip" class="metricCard kpiCard">
                  <content>
                    <VBox class="metricContent">
                      <HBox justifyContent="SpaceBetween" alignItems="Center">
                        <ObjectStatus title="Title" text="{= ${appState>/currentTrip/title} || 'No active trip'}" />
                        <core:Icon src="sap-icon://journey-change" class="kpiIcon" />
                      </HBox>
                      <ObjectStatus title="Status" text="{= ${appState>/currentTrip/status} || 'IDLE'}" state="{= ${appState>/currentTrip/status} === 'ACTIVE' ? 'Success' : 'Information' }" />
                      <ObjectStatus title="Started At" text="{= ${appState>/currentTrip/startedAt} || '-'}" />
                    </VBox>
                  </content>
                </Panel>

                <Panel headerText="Live Position" class="metricCard kpiCard">
                  <content>
                    <VBox class="metricContent">
                      <HBox justifyContent="SpaceBetween" alignItems="Center">
                        <Title text="Latest Coordinates" level="H5" />
                        <core:Icon src="sap-icon://map-3" class="kpiIcon" />
                      </HBox>
                      <ObjectStatus title="Latitude" text="{= ${appState>/lastPoint/latitude} || '-'}" />
                      <ObjectStatus title="Longitude" text="{= ${appState>/lastPoint/longitude} || '-'}" />
                      <ObjectStatus title="Accuracy (m)" text="{= ${appState>/lastPoint/accuracy} || '-'}" />
                    </VBox>
                  </content>
                </Panel>

                <Panel headerText="Captured Points" class="metricCard kpiCard">
                  <content>
                    <VBox class="metricContent">
                      <HBox justifyContent="SpaceBetween" alignItems="Center">
                        <Title text="Path Density" level="H5" />
                        <core:Icon src="sap-icon://line-chart" class="kpiIcon" />
                      </HBox>
                      <ObjectNumber number="{appState>/totalPoints}" unit="pts" />
                      <ProgressIndicator percentValue="{= ${appState>/totalPoints} > 500 ? 100 : ${appState>/totalPoints} / 5 }" displayValue="{= ${appState>/totalPoints} + ' points captured'}" state="Success" />
                    </VBox>
                  </content>
                </Panel>
              </HBox>

              <Panel headerText="Performance Metrics" class="metricCard metricsPanel">
                <content>
                  <HBox wrap="Wrap" class="cardRow">
                    <VBox class="metricContent metricColumn">
                      <ObjectStatus title="Total Trips" text="{appState>/metrics/totalTrips}" />
                      <ObjectStatus title="Completed Trips" text="{appState>/metrics/completedTrips}" />
                      <ObjectStatus title="Completion Rate" text="{= ${appState>/metrics/completionRate} + '%'}" state="Success" />
                      <ObjectStatus title="Avg Points / Trip" text="{appState>/metrics/avgPointsPerTrip}" />
                      <ObjectStatus title="Avg GPS Accuracy (m)" text="{appState>/metrics/avgGpsAccuracy}" />
                    </VBox>
                    <VBox class="metricContent metricColumn">
                      <ObjectStatus title="Avg Session Duration (ms)" text="{appState>/metrics/avgSessionDurationMs}" />
                      <ObjectStatus title="Backend Ingest Success" text="{= ${appState>/metrics/ingestSuccessRate} + '%'}" state="Success" />
                      <ObjectStatus title="Avg Backend Ingest Latency (ms)" text="{appState>/metrics/avgIngestLatencyMs}" />
                      <ObjectStatus title="Avg Client Update Latency (ms)" text="{appState>/metrics/avgClientUpdateLatencyMs}" />
                      <ObjectStatus title="Latest Client Update Latency (ms)" text="{appState>/metrics/latestClientUpdateLatencyMs}" />
                    </VBox>
                  </HBox>
                </content>
              </Panel>
            </VBox>
          </VBox>
        </content>
      </Page>

      <Page id="fleetOverviewPage" title="Fleet Management" showNavButton="true" navButtonPress=".onNavigateBackToDashboard">
        <content>
          <VBox class="trackerPage">
            <Toolbar>
              <Button text="Add Truck" icon="sap-icon://add" type="Emphasized" press=".onOpenAddTruckDialog" />
              <Button text="Refresh" icon="sap-icon://refresh" press=".onRefreshFleetOverview" />
              <ToolbarSpacer />
              <SearchField placeholder="Search truck #, model or registration…" width="22rem" liveChange=".onTruckSearch" />
            </Toolbar>
            <HBox wrap="Wrap" class="cardRow kpiRow">
              <VBox class="metricCard kpiCard kpiCardBlue">
                <VBox class="kpiCardContent">
                  <HBox alignItems="Center" class="kpiCardHeader">
                    <core:Icon src="sap-icon://fleet" class="kpiCardIcon kpiIconBlue" />
                    <Text text="Total Trucks" class="kpiCardLabel sapUiSmallMarginBegin" />
                  </HBox>
                  <HBox alignItems="Baseline">
                    <Text text="{appState>/fleetSummary/total}" class="kpiCardBigNumber" />
                    <Text text="trucks" class="kpiCardUnit" />
                  </HBox>
                </VBox>
              </VBox>
              <VBox class="metricCard kpiCard kpiCardGreen">
                <VBox class="kpiCardContent">
                  <HBox alignItems="Center" class="kpiCardHeader">
                    <core:Icon src="sap-icon://accept" class="kpiCardIcon kpiIconGreen" />
                    <Text text="Active" class="kpiCardLabel sapUiSmallMarginBegin" />
                  </HBox>
                  <Text text="{appState>/fleetSummary/active}" class="kpiCardBigNumber kpiNumberGreen" />
                </VBox>
              </VBox>
              <VBox class="metricCard kpiCard kpiCardOrange">
                <VBox class="kpiCardContent">
                  <HBox alignItems="Center" class="kpiCardHeader">
                    <core:Icon src="sap-icon://pause" class="kpiCardIcon kpiIconOrange" />
                    <Text text="Idle" class="kpiCardLabel sapUiSmallMarginBegin" />
                  </HBox>
                  <Text text="{appState>/fleetSummary/idle}" class="kpiCardBigNumber kpiNumberOrange" />
                </VBox>
              </VBox>
              <VBox class="metricCard kpiCard kpiCardRed">
                <VBox class="kpiCardContent">
                  <HBox alignItems="Center" class="kpiCardHeader">
                    <core:Icon src="sap-icon://cancel" class="kpiCardIcon kpiIconRed" />
                    <Text text="Deactivated" class="kpiCardLabel sapUiSmallMarginBegin" />
                  </HBox>
                  <Text text="{appState>/fleetSummary/deactivated}" class="kpiCardBigNumber kpiNumberRed" />
                </VBox>
              </VBox>
            </HBox>
            <Table id="fleetTable" items="{appState>/trucks}" mode="SingleSelectMaster" selectionChange=".onSelectTruck">
              <columns>
                <Column><Text text="Truck #" /></Column>
                <Column><Text text="Model" /></Column>
                <Column><Text text="Registration" /></Column>
                <Column><Text text="Fuel Type" /></Column>
                <Column><Text text="Status" /></Column>
                <Column><Text text="Assigned Driver" /></Column>
                <Column hAlign="Center"><Text text="Actions" /></Column>
              </columns>
              <items>
                <ColumnListItem>
                  <cells>
                    <Text text="{appState>truckNumber}" />
                    <Text text="{appState>model}" />
                    <Text text="{appState>registrationNumber}" />
                    <Text text="{appState>fuelType}" />
                    <ObjectStatus
                      text="{appState>status}"
                      state="{= ${appState>status} === 'ACTIVE' ? 'Success' : ${appState>status} === 'ON_TRIP' ? 'Warning' : ${appState>status} === 'IDLE' ? 'None' : 'Error' }" />
                    <Text text="{appState>assignedDriverName}" />
                    <HBox justifyContent="Center">
                      <Button icon="sap-icon://person-placeholder" tooltip="Assign Driver" press=".onOpenAssignDriverDialog">
                        <customData>
                          <core:CustomData key="truckId" value="{appState>ID}" />
                        </customData>
                      </Button>
                      <Button icon="sap-icon://detail-view" tooltip="View Details" press=".onNavigateToTruckDetail">
                        <customData>
                          <core:CustomData key="truckId" value="{appState>ID}" />
                        </customData>
                      </Button>
                      <Button icon="sap-icon://cancel" tooltip="Deactivate" press=".onDeactivateTruck" visible="{= ${appState>status} !== 'DEACTIVATED'}">
                        <customData>
                          <core:CustomData key="truckId" value="{appState>ID}" />
                        </customData>
                      </Button>
                    </HBox>
                  </cells>
                </ColumnListItem>
              </items>
            </Table>
          </VBox>
        </content>
      </Page>

      <Page id="freightOrdersPage" title="Freight Orders" showNavButton="true" navButtonPress=".onNavigateBackToDashboard">
        <subHeader>
          <OverflowToolbar>
            <Title text="Freight Orders" />
            <ToolbarSpacer />
            <Select
              id="statusFilterSelect"
              selectedKey="{appState>/freightOrderStatusFilter}"
              change=".onFreightStatusFilterChange">
              <items>
                <core:Item key="ALL" text="All Orders" />
                <core:Item key="PLANNED" text="Planned" />
                <core:Item key="DISPATCHED" text="Dispatched" />
                <core:Item key="DELIVERED" text="Delivered" />
                <core:Item key="CANCELLED" text="Cancelled" />
              </items>
            </Select>
            <Button text="+ New Order" type="Emphasized" press=".onOpenFreightOrderDialog" />
            <Button icon="sap-icon://refresh" press=".onRefreshFreightOrders" />
          </OverflowToolbar>
        </subHeader>
        <content>
          <VBox class="sectionCard">
            <HBox wrap="Wrap" class="cardRow kpiRow">
              <VBox class="metricCard kpiCard kpiCardNeutral">
                <VBox class="kpiCardContent">
                  <HBox alignItems="Center" class="kpiCardHeader">
                    <core:Icon src="sap-icon://document-list" class="kpiCardIcon kpiIconNeutral" />
                    <Text text="Total Orders" class="kpiCardLabel sapUiSmallMarginBegin" />
                  </HBox>
                  <Text text="{appState>/freightOrderCounts/total}" class="kpiCardBigNumber" />
                </VBox>
              </VBox>
              <VBox class="metricCard kpiCard kpiCardBlue">
                <VBox class="kpiCardContent">
                  <HBox alignItems="Center" class="kpiCardHeader">
                    <core:Icon src="sap-icon://calendar" class="kpiCardIcon kpiIconBlue" />
                    <Text text="Planned" class="kpiCardLabel sapUiSmallMarginBegin" />
                  </HBox>
                  <Text text="{appState>/freightOrderCounts/planned}" class="kpiCardBigNumber kpiNumberBlue" />
                </VBox>
              </VBox>
              <VBox class="metricCard kpiCard kpiCardOrange">
                <VBox class="kpiCardContent">
                  <HBox alignItems="Center" class="kpiCardHeader">
                    <core:Icon src="sap-icon://shipping-status" class="kpiCardIcon kpiIconOrange" />
                    <Text text="Dispatched" class="kpiCardLabel sapUiSmallMarginBegin" />
                  </HBox>
                  <Text text="{appState>/freightOrderCounts/dispatched}" class="kpiCardBigNumber kpiNumberOrange" />
                </VBox>
              </VBox>
              <VBox class="metricCard kpiCard kpiCardGreen">
                <VBox class="kpiCardContent">
                  <HBox alignItems="Center" class="kpiCardHeader">
                    <core:Icon src="sap-icon://accept" class="kpiCardIcon kpiIconGreen" />
                    <Text text="Delivered" class="kpiCardLabel sapUiSmallMarginBegin" />
                  </HBox>
                  <Text text="{appState>/freightOrderCounts/delivered}" class="kpiCardBigNumber kpiNumberGreen" />
                </VBox>
              </VBox>
            </HBox>

            <Table
              id="freightOrdersTable"
              items="{appState>/freightOrders}"
              mode="SingleSelectMaster"
              selectionChange=".onSelectFreightOrder"
              noDataText="No freight orders found">
              <columns>
                <Column><Text text="Order #" /></Column>
                <Column><Text text="Truck" /></Column>
                <Column><Text text="Driver" /></Column>
                <Column><Text text="Origin" /></Column>
                <Column><Text text="Destination" /></Column>
                <Column><Text text="Planned Departure" /></Column>
                <Column><Text text="Planned Arrival" /></Column>
                <Column><Text text="Checkpoints" /></Column>
                <Column><Text text="Status" /></Column>
                <Column hAlign="Center"><Text text="Actions" /></Column>
              </columns>
              <items>
                <ColumnListItem>
                  <cells>
                    <Text text="{appState>orderNumber}" />
                    <Text text="{appState>truckNumber}" />
                    <Text text="{appState>driverName}" />
                    <Text text="{appState>origin}" />
                    <Text text="{appState>destination}" />
                    <Text text="{path: 'appState>plannedDeparture', formatter: '.formatDateTime'}" />
                    <Text text="{path: 'appState>plannedArrival', formatter: '.formatDateTime'}" />
                    <HBox>
                      <Text text="{appState>checkpointCount}" />
                      <Text text="required" class="sapUiTinyMarginBegin" />
                    </HBox>
                    <ObjectStatus
                      text="{appState>status}"
                      state="{= ${appState>status} === 'DELIVERED' ? 'Success' : ${appState>status} === 'DISPATCHED' ? 'Warning' : ${appState>status} === 'PLANNED' ? 'None' : ${appState>status} === 'CANCELLED' ? 'Error' : 'None'}" />
                    <HBox justifyContent="Center">
                      <Button icon="sap-icon://car-rental" tooltip="Dispatch" type="Accept" press=".onDispatchFreightOrder" visible="{= ${appState>status} === 'PLANNED'}">
                        <customData>
                          <core:CustomData key="orderId" value="{appState>ID}" />
                        </customData>
                      </Button>
                      <Button icon="sap-icon://detail-view" tooltip="View Checkpoints" press=".onViewCheckpoints" visible="{= ${appState>status} !== 'PLANNED'}">
                        <customData>
                          <core:CustomData key="orderId" value="{appState>ID}" />
                        </customData>
                      </Button>
                      <Button icon="sap-icon://edit" tooltip="Edit" press=".onEditFreightOrder" visible="{= ${appState>status} === 'PLANNED'}">
                        <customData>
                          <core:CustomData key="orderId" value="{appState>ID}" />
                        </customData>
                      </Button>
                    </HBox>
                  </cells>
                </ColumnListItem>
              </items>
            </Table>
          </VBox>
        </content>
      </Page>

      <Page id="gatePassesPage" title="Gate Passes" showNavButton="true" navButtonPress=".onNavigateBackToDashboard">
        <subHeader>
          <OverflowToolbar>
            <Title text="Gate Pass Register" />
            <ToolbarSpacer />
            <Select
              id="gatePassTruckFilter"
              selectedKey="{appState>/gatePassFilter/truckId}"
              change=".onGatePassFilterChange"
              forceSelection="false"
              items="{appState>/trucks}">
              <items>
                <core:Item key="{appState>ID}" text="{appState>truckNumber}" />
              </items>
            </Select>
            <Select
              id="gatePassOrderFilter"
              selectedKey="{appState>/gatePassFilter/freightOrderId}"
              change=".onGatePassFilterChange"
              forceSelection="false"
              items="{appState>/freightOrders}">
              <items>
                <core:Item key="{appState>ID}" text="{appState>orderNumber}" />
              </items>
            </Select>
            <Button text="Clear Filters" press=".onClearGatePassFilters" />
            <Button text="+ Log Gate Pass" type="Emphasized" press=".onOpenGatePassDialog" />
            <Button icon="sap-icon://refresh" press=".onRefreshGatePasses" />
          </OverflowToolbar>
        </subHeader>
        <content>
          <VBox class="sectionCard">
            <HBox wrap="Wrap" class="cardRow kpiRow">
              <VBox class="metricCard kpiCard kpiCardNeutral">
                <VBox class="kpiCardContent">
                  <HBox alignItems="Center" class="kpiCardHeader">
                    <core:Icon src="sap-icon://log" class="kpiCardIcon kpiIconNeutral" />
                    <Text text="Total Passes" class="kpiCardLabel sapUiSmallMarginBegin" />
                  </HBox>
                  <Text text="{appState>/gatePassCounts/total}" class="kpiCardBigNumber" />
                </VBox>
              </VBox>
              <VBox class="metricCard kpiCard kpiCardOrange">
                <VBox class="kpiCardContent">
                  <HBox alignItems="Center" class="kpiCardHeader">
                    <core:Icon src="sap-icon://arrow-right" class="kpiCardIcon kpiIconOrange" />
                    <Text text="Trucks OUT" class="kpiCardLabel sapUiSmallMarginBegin" />
                  </HBox>
                  <Text text="{appState>/gatePassCounts/out}" class="kpiCardBigNumber kpiNumberOrange" />
                </VBox>
              </VBox>
              <VBox class="metricCard kpiCard kpiCardGreen">
                <VBox class="kpiCardContent">
                  <HBox alignItems="Center" class="kpiCardHeader">
                    <core:Icon src="sap-icon://arrow-left" class="kpiCardIcon kpiIconGreen" />
                    <Text text="Trucks IN" class="kpiCardLabel sapUiSmallMarginBegin" />
                  </HBox>
                  <Text text="{appState>/gatePassCounts/in}" class="kpiCardBigNumber kpiNumberGreen" />
                </VBox>
              </VBox>
              <VBox class="metricCard kpiCard kpiCardRed">
                <VBox class="kpiCardContent">
                  <HBox alignItems="Center" class="kpiCardHeader">
                    <core:Icon src="sap-icon://pending" class="kpiCardIcon kpiIconRed" />
                    <Text text="Pending" class="kpiCardLabel sapUiSmallMarginBegin" />
                  </HBox>
                  <Text text="{appState>/gatePassCounts/pending}" class="{= ${appState>/gatePassCounts/pending} > 0 ? 'kpiCardBigNumber kpiNumberRed' : 'kpiCardBigNumber' }" />
                </VBox>
              </VBox>
            </HBox>

            <Table
              id="gatePassesTable"
              items="{appState>/gatePasses}"
              noDataText="No gate pass records found"
              growing="true"
              growingThreshold="25">
              <columns>
                <Column><Text text="Freight Order" /></Column>
                <Column><Text text="Truck" /></Column>
                <Column><Text text="Driver" /></Column>
                <Column><Text text="Direction" /></Column>
                <Column><Text text="Gate Officer" /></Column>
                <Column><Text text="Passed At" /></Column>
                <Column><Text text="Remarks" /></Column>
                <Column><Text text="Status" /></Column>
              </columns>
              <items>
                <ColumnListItem>
                  <cells>
                    <Text text="{appState>orderNumber}" />
                    <Text text="{appState>truckNumber}" />
                    <Text text="{appState>driverName}" />
                    <ObjectStatus
                      text="{appState>direction}"
                      state="{= ${appState>direction} === 'OUT' ? 'Warning' : 'Success'}"
                      icon="{= ${appState>direction} === 'OUT' ? 'sap-icon://arrow-right' : 'sap-icon://arrow-left'}" />
                    <Text text="{appState>gateOfficer}" />
                    <Text text="{path: 'appState>passedAt', formatter: '.formatDateTime'}" />
                    <Text text="{appState>remarks}" />
                    <ObjectStatus
                      text="{appState>status}"
                      state="{= ${appState>status} === 'APPROVED' ? 'Success' : ${appState>status} === 'PENDING' ? 'Warning' : 'Error'}" />
                  </cells>
                </ColumnListItem>
              </items>
            </Table>
          </VBox>
        </content>
      </Page>

      <Page id="truckDetailPage" title="Truck - {appState>/selectedTruck/truckNumber}" showNavButton="true" navButtonPress=".onNavigateBackToFleet">
        <content>
          <IconTabBar selectedKey="details" select=".onTruckDetailTabSelect">
            <items>
              <IconTabFilter key="details" text="Details">
                <VBox class="sapUiSmallMargin">
                  <form:SimpleForm editable="true" layout="ResponsiveGridLayout">
                    <Label text="Truck Number" />
                    <Input value="{appState>/selectedTruck/truckNumber}" />
                    <Label text="Model" />
                    <Input value="{appState>/selectedTruck/model}" />
                    <Label text="Registration" />
                    <Input value="{appState>/selectedTruck/registrationNumber}" />
                    <Label text="Fuel Type" />
                    <Select selectedKey="{appState>/selectedTruck/fuelType}">
                      <items>
                        <core:Item key="Petrol" text="Petrol" />
                        <core:Item key="Diesel" text="Diesel" />
                        <core:Item key="Electric" text="Electric" />
                        <core:Item key="CNG" text="CNG" />
                      </items>
                    </Select>
                    <Label text="Status" />
                    <Select selectedKey="{appState>/selectedTruck/status}">
                      <items>
                        <core:Item key="IDLE" text="IDLE" />
                        <core:Item key="ACTIVE" text="ACTIVE" />
                        <core:Item key="ON_TRIP" text="ON_TRIP" />
                        <core:Item key="IN_MAINTENANCE" text="IN_MAINTENANCE" />
                        <core:Item key="DEACTIVATED" text="DEACTIVATED" />
                      </items>
                    </Select>
                    <Label text="Assigned Driver" />
                    <Text text="{appState>/selectedTruck/assignedDriverName}" />
                  </form:SimpleForm>
                  <Toolbar>
                    <Button text="Save Changes" type="Emphasized" press=".onSaveTruckDetails" />
                    <Button text="Assign Driver" press=".onOpenAssignDriverDialog">
                      <customData>
                        <core:CustomData key="truckId" value="{appState>/selectedTruckId}" />
                      </customData>
                    </Button>
                  </Toolbar>
                </VBox>
              </IconTabFilter>
              <IconTabFilter key="metrics" text="Vehicle Metrics">
                <VBox class="sapUiSmallMargin">
                  <Toolbar>
                    <DateRangeSelection id="metricsDateRange" />
                    <Button text="Load" press=".onLoadTruckMetrics" />
                  </Toolbar>
                  <HBox wrap="Wrap" class="cardRow kpiRow">
                    <ObjectStatus title="Avg Fuel (L)" text="{appState>/truckMetricsSummary/avgFuelLitres}" />
                    <ObjectStatus title="Min Tyre (PSI)" text="{appState>/truckMetricsSummary/minTyrePressure}" />
                    <ObjectStatus title="Max Temp (C)" text="{appState>/truckMetricsSummary/maxEngineTemp}" />
                    <ObjectStatus title="Total Readings" text="{appState>/truckMetricsSummary/totalReadings}" />
                  </HBox>
                  <Table id="metricsTable" items="{appState>/truckMetrics}">
                    <columns>
                      <Column><Text text="Captured At" /></Column>
                      <Column><Text text="Fuel (L)" /></Column>
                      <Column><Text text="FL PSI" /></Column>
                      <Column><Text text="FR PSI" /></Column>
                      <Column><Text text="RL PSI" /></Column>
                      <Column><Text text="RR PSI" /></Column>
                      <Column><Text text="Temp C" /></Column>
                      <Column><Text text="Odometer km" /></Column>
                      <Column><Text text="Source" /></Column>
                    </columns>
                    <items>
                      <ColumnListItem>
                        <cells>
                          <Text text="{path: 'appState>capturedAt', formatter: '.formatDateTime'}" />
                          <Text text="{appState>fuelLitres}" />
                          <Text text="{appState>tyreFL}" />
                          <Text text="{appState>tyreFR}" />
                          <Text text="{appState>tyreRL}" />
                          <Text text="{appState>tyreRR}" />
                          <Text text="{appState>engineTempC}" />
                          <Text text="{appState>odometerKm}" />
                          <Text text="{appState>source}" />
                        </cells>
                      </ColumnListItem>
                    </items>
                  </Table>
                </VBox>
              </IconTabFilter>
              <IconTabFilter key="thresholds" text="Alert Thresholds">
                <VBox class="sapUiSmallMargin">
                  <Text text="Thresholds are evaluated every 5 minutes against the latest vehicle metric reading." />
                  <form:SimpleForm editable="true" layout="ResponsiveGridLayout">
                    <Title text="Fuel Level (%)" />
                    <Label text="Warning below" />
                    <StepInput value="{appState>/thresholds/FUEL_LEVEL/warningAt}" min="0" max="100" step="1" />
                    <Label text="Critical below" />
                    <StepInput value="{appState>/thresholds/FUEL_LEVEL/criticalAt}" min="0" max="100" step="1" />
                    <Title text="Tyre Pressure (PSI)" />
                    <Label text="Warning below" />
                    <StepInput value="{appState>/thresholds/TYRE_PRESSURE/warningAt}" min="0" max="120" step="1" />
                    <Label text="Critical below" />
                    <StepInput value="{appState>/thresholds/TYRE_PRESSURE/criticalAt}" min="0" max="120" step="1" />
                    <Title text="Engine Temperature (C)" />
                    <Label text="Warning above" />
                    <StepInput value="{appState>/thresholds/ENGINE_TEMP/warningAt}" min="0" max="150" step="1" />
                    <Label text="Critical above" />
                    <StepInput value="{appState>/thresholds/ENGINE_TEMP/criticalAt}" min="0" max="150" step="1" />
                  </form:SimpleForm>
                  <Toolbar>
                    <Button text="Save Thresholds" type="Emphasized" press=".onSaveThresholds" />
                  </Toolbar>
                </VBox>
              </IconTabFilter>
              <IconTabFilter key="freightHistory" text="Freight History">
                <Table id="truckFreightTable" items="{appState>/truckFreightHistory}">
                  <columns>
                    <Column><Text text="Order #" /></Column>
                    <Column><Text text="Origin" /></Column>
                    <Column><Text text="Destination" /></Column>
                    <Column><Text text="Planned Departure" /></Column>
                    <Column><Text text="Status" /></Column>
                    <Column><Text text="Checkpoints" /></Column>
                    <Column><Text text="Driver" /></Column>
                  </columns>
                  <items>
                    <ColumnListItem>
                      <cells>
                        <Text text="{appState>orderNumber}" />
                        <Text text="{appState>origin}" />
                        <Text text="{appState>destination}" />
                        <Text text="{path: 'appState>plannedDeparture', formatter: '.formatDateTime'}" />
                        <ObjectStatus
                          text="{appState>status}"
                          state="{= ${appState>status} === 'ACTIVE' ? 'Success' : ${appState>status} === 'ON_TRIP' ? 'Warning' : ${appState>status} === 'IDLE' ? 'None' : 'Error' }" />
                        <Text text="{appState>checkpointCount}" />
                        <Text text="{appState>driverName}" />
                      </cells>
                    </ColumnListItem>
                  </items>
                </Table>
              </IconTabFilter>
            </items>
          </IconTabBar>
        </content>
      </Page>
    </pages>
  </App>
</mvc:View>

