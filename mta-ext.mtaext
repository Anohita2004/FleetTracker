_schema-version: "3.3.0"
ID: locationtracker
extends: locationtracker

parameters:
  # The JWT_SECRET_KEY should be injected at deployment time via CI/CD or platform secrets manager
  # (e.g. using `cf set-env locationtracker-srv JWT_SECRET_KEY "..."`)
  JWT_SECRET_KEY: ~{JWT_SECRET_KEY}



